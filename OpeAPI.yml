info:
  nom: STD23097 - TD1
  version: 1.0.0
server:
  URL: "https://localhost.STD23097.com"
path:
  /customers:
    get:
      response:
        200:
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/...."
  components:
    schema:
      customer:
        type: object
        properties:
          name:
            type: String
          age:
            type: Integer
